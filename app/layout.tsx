import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Emmanuel Success – Web & Mobile App Developer",
  description:
    "Portfolio of Emmanuel Success, a web and mobile app developer building modern websites and cross-platform mobile apps using React, Next.js, React Native, Tailwind CSS, and scalable backend technologies like Nodejs Prisma and Postgresql. Explore projects, skills, and experience.",
};


export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
